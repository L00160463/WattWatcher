@page "/ViewLive"
@inject NavigationManager NavigationManager

<h3 class="text-center">Dashboard</h3> <!-- Added mt-5 and pt-3 to add top margin and padding -->

<div class="container mt-4">
    <!-- Added mt-4 to ensure there is space between the navbar and the container -->
    <div class="cards-grid">
        <Card Class="mb-4 dashboard-card" Style="width:18rem;">
            <CardBody>
                <div class="card-icon bg-primary"><i class="bi bi-house-door"></i></div>
                <CardTitle>Entire Home</CardTitle>
                <CardSubTitle Class="mb-2 text-muted">€104.53 this month</CardSubTitle>
                <CardText>Let's aim to reduce our footprint and costs from last month's baseline.</CardText>
                <Button Color="ButtonColor.Primary" To="/" Type="ButtonType.Link">View More</Button>
            </CardBody>
        </Card>

        <Card Class="mb-4 dashboard-card" Style="width:18rem;">
            <CardBody>
                <div class="card-icon bg-primary"><i class="bi bi-lamp-fill"></i></div>
                <CardTitle>Living Room</CardTitle>
                <CardSubTitle Class="mb-2 text-muted">€22.67 this month</CardSubTitle>
                <CardText>Moderate energy use here. Save more with LED bulbs and unplugging idle devices.</CardText>
                <Button Color="ButtonColor.Primary" To="/Circuit1" Type="ButtonType.Link">View More</Button>
            </CardBody>
        </Card>

        <Card Class="mb-4 dashboard-card" Style="width:18rem;">
            <CardBody>
                <div class="card-icon bg-primary"><i class="bi bi-hammer"></i></div>
                <CardTitle>Garage</CardTitle>
                <CardSubTitle Class="mb-2 text-muted">€32.85 this month</CardSubTitle>
                <CardText>High energy hub due to appliances. Save by using them during off-peak hours.</CardText>
                <Button Color="ButtonColor.Primary" To="/Circuit2" Type="ButtonType.Link">View More</Button>
            </CardBody>
        </Card>

        <Card Class="mb-4 dashboard-card" Style="width:18rem;">
            <CardBody>
                <div class="card-icon bg-primary"><i class="bi bi-fire"></i></div>
                <CardTitle>Central Heating</CardTitle>
                <CardSubTitle Class="mb-2 text-muted">€22.63 this month</CardSubTitle>
                <CardText>Keep cozy efficiently. Service your system and insulate your home to save.</CardText>
                <Button Color="ButtonColor.Primary" To="/Circuit3" Type="ButtonType.Link">View More</Button>
            </CardBody>
        </Card>

        <Card Class="mb-4 dashboard-card" Style="width:18rem;">
            <CardBody>
                <div class="card-icon bg-primary"><i class="bi bi-moon-fill"></i></div>
                <CardTitle>Master Bedroom</CardTitle>
                <CardSubTitle Class="mb-2 text-muted">€14.56 this month</CardSubTitle>
                <CardText>Low energy usage. Opt for natural light and energy-saving device modes at night.</CardText>
                <Button Color="ButtonColor.Primary" To="/Circuit4" Type="ButtonType.Link">View More</Button>
            </CardBody>
        </Card>
    </div>
</div>
